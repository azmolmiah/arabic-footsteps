import React, { Fragment } from 'react';

import Header from '../Layout/HomePage/Header';

const HomePage = () => {
  return (
    <Fragment>
      <Header />
    </Fragment>
  );
};

export default HomePage;
